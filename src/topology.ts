/* import { Segment } from './segment';

import { checkIntersection } from 'line-intersect';
import { combinationsOnce } from './aux';

export function segmentsToGraph(segments:Segment[]) {
    const segmentIndices = combinationsOnce(segments.length, false);
    for (let [si1, si2] of segmentIndices) {
        const seg1 = segments[si1];
        const seg2 = segments[si2];

        if (si1 === si2) {

        } else {

        }
    }
}
 */
